{
  "name": "dijinew-frontend",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "build": "npm run build --prefix client",
    "start": "npm run preview --prefix client",
    "dev": "npm run dev --prefix client",
    "vercel-build": "cd client && npm install && npm run vercel-build"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "qrcode.react": "^4.2.0"
  }
}